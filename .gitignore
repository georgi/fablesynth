.DS_Store
node_modules
dist
*.tsbuildinfo

# JUCE / C++ build artifacts
juce/build/
juce/build-*/
build/

# Redundant compressed copy of design-handoff/ (the extracted dir is tracked)
Synthesizer mockup design-handoff.zip

# Claude Code session state (agent worktrees, local settings)
.claude/

# Offscreen editor snapshots written by the JUCE host tests
*_editor.png
